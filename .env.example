# Senha para criptografar a sessão (cookie selado). Mínimo de 32 caracteres.
# Gere uma: openssl rand -base64 32
NUXT_SESSION_PASSWORD=secret

# PostgreSQL (Drizzle ORM)
DATABASE_URL=postgresql://glm:glm@localhost:5432/glm
